<footer class="foot">
    <div>
    <% if current_user && current_user.admin %>   
        <a href="/orders" class "btn btn-block btn-info active btn-embossed "> View Orders</a>
        
     <% end %>
 
    <% if user_signed_in? %>
        <%= link_to 'Update profile', edit_user_registration_path %>
    <% end %>
</footer>